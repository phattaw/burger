<h1>Eat-Da-Burger!</h1>

<h2>Tasty burgers for you to eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you've tried!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Ready to pick?</label><br>
    <input type="radio" name="devoured" value="0" checked>Ready to eat<br>
    <input type="radio" name="devoured" value="1">Eaten!
  </div>

  <button type="submit">Add Burger</button>
</form>
